import { createTheme } from "@mui/material";

const theme = createTheme({
  components: {
    
  },
  palette: {
    primary: {
      main: '#0047ab'
    },
    background: {
      default: '#bfd1e5'
    }
  },
  typography: {
    fontFamily: 'serif',
    color: '#bfd1e5'
  },
})

export default theme